\begin{frame}[fragile]{}
\centering

{\scriptsize
\begin{lstlisting}[language=Python]
config = PPOConfig()
config.environment("SpaceInvaders")
config.rl_module(
    model_config={
        "conv_filters": [
            [32, [8, 8], 4],
            [64, [4, 4], 2],
            [64, [3, 3], 1]
        ],
    }
)

algo = config.build()
print(algo.train())
print(algo.evaluate())
algo.stop()
\end{lstlisting}
}

\end{frame}
